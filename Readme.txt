The pattern of the no of strings of length n without two consecutive 0's is identical to pattern of fibonacci numbers,
where the count is actually (n+2)'th Fibonacci number for n>=1.

For example:
	if
	n=1, count=2=fib(3)
	n=2, count=3=fib(4)
	n=3, count=5=fib(5)
	

